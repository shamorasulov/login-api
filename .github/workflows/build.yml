name: Build and Test .NET 10 App

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build the project
      run: dotnet build --configuration Release

    # NEW: This runs the tests we worked on yesterday
    - name: Run Tests
      run: dotnet test --no-build --configuration Release